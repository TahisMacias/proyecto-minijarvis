# Mini-JARVIS — dependencias fijadas y verificadas (ver .agents/AGENTS.md, T-01)
# No actualizar sin volver a verificar el stack completo.

# --- Utilidades base ---
python-dotenv==1.2.2

# --- Pipeline de voz (STT / TTS / audio) ---
openai==3.0.0
edge-tts==7.2.8
sounddevice==0.5.5

# --- GUI de escritorio ---
customtkinter==6.0.0

# Pillow lo importa directamente gui/desktop_app.py ("from PIL import Image") para
# cargar el mapa de atencion en la pestana Laboratorio. Hasta la auditoria del
# 2026-08-14 no estaba declarado aqui: se instalaba de rebote porque matplotlib
# depende de el. Eso hacia que la version quedara suelta y que la ventana dependiera
# de una dependencia ajena. Lo que se importa se declara y se fija.
pillow==12.3.0

# --- Reservadas para tareas AUN NO IMPLEMENTADAS ---
# Estas tres se fijan por adelantado, pero hoy NO las importa ningun archivo del
# proyecto. Se dejan declaradas para que la instalacion no cambie cuando se abran esas
# tareas, y se dice aqui que estan sin usar para que nadie afirme lo contrario en el
# informe ni en la sustentacion.
#
#   psutil, duckduckgo-search  ->  herramientas de Tool Calling (T-15, Fase 2).
#
#   tiktoken  ->  reservada para T-14. AVISO: hasta la revision del 2026-08-23 el
#   comentario de este archivo decia que tiktoken servia para "el conteo de tokens del
#   indicador de memoria". Era falso. core/memory.py NO usa ningun tokenizador: estima
#   con la regla de 4 caracteres por token y documenta a proposito por que (truncar por
#   turnos es predecible y se prueba sin red; la cuenta exacta solo la sabe el
#   proveedor). Si T-14 termina necesitando la cuenta exacta, esta fijada; si no, esta
#   linea se puede borrar sin tocar una sola linea de codigo.
tiktoken==0.13.0

# --- Pruebas (gates deterministas del plan: T-04, T-09, T-15) ---
pytest==9.1.1

# --- Exploracion del Transformer (tokenizacion, embeddings, self-attention) ---
matplotlib==3.11.1
transformers==5.15.0
torch==2.13.0
numpy==2.5.2

# --- Modo sin internet (T-21) ---
# Respaldo local para cuando no hay conexion: oir, pensar y hablar en esta maquina.
# El modelo de lenguaje local NO necesita libreria nueva: usa torch y transformers, que
# ya estaban para el laboratorio del Transformer. Se intento primero llama-cpp-python,
# que es lo habitual para esto, y NO tiene rueda para Python 3.14: compilarlo desde
# codigo fuente en Windows era exactamente el riesgo que este proyecto no puede correr
# a tres dias de la entrega.
#
# Los MODELOS no vienen aqui: se descargan con `python -m core.modo_local`, una vez y
# con internet. Son unos 700 MB entre los dos.
faster-whisper==1.2.1
pyttsx3==2.99
