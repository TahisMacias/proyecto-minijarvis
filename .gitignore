# --- Credenciales: NUNCA versionar (requisito seccion 6 del PDF) ---
.env
.env.*
!.env.example
*.key
credentials.json
token.json

# --- Material del docente: no republicar en un repo publico ---
# El enunciado es documento del docente. Se conserva en local, fuera de Git.
Proyecto_MiniJARVIS.pdf

# --- Entorno virtual ---
.venv/
venv/
env/

# --- Python ---
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.mypy_cache/

# --- Registro de arranque cuando se abre con doble clic ---
# El lanzador usa pythonw, que no tiene consola: los mensajes de arranque van
# a este archivo en vez de perderse. Se regenera en cada arranque.
registro-arranque.txt

# --- Audio / artefactos temporales generados en runtime ---
*.wav
*.mp3
capturas/
temp/

# --- Modelos y caches de Hugging Face descargados localmente ---
.cache/
models/

# --- OneDrive / Windows ---
desktop.ini
Thumbs.db
*.tmp
~$*

# --- Editores ---
.vscode/
.idea/
